{
  "project": "mint-learn",
  "description": "MintLearn 去中心化课程平台 — 按 DevPlan.md 阶段拆分的任务清单",
  "tasks": [
    {
      "name": "C-01-mttoken-tests",
      "coding_prompt": "在 packages/contracts/test/MTToken.test.ts 中为 MTToken.sol 编写完整单元测试。\n\n合约位置: packages/contracts/contracts/MTToken.sol\n测试框架: Hardhat + ethers.js + Chai\n\n必须覆盖以下用例:\n1. 部署后 owner 持有全部初始供应量（1_000_000_000 * 10^18）\n2. owner 可以调用 mint(address, amount) 给任意地址增发\n3. 非 owner 调用 mint 应 revert（OwnableUnauthorizedAccount）\n4. 任意持有者可以调用 burn(amount) 销毁自己的代币\n5. burn 金额超过余额时应 revert\n6. ERC20 标准: transfer、transferFrom、approve、allowance 行为正确\n7. 转账到 address(0) 应 revert\n\n运行命令: cd packages/contracts && pnpm test",
      "acceptance_criteria": "1. 所有测试用例均通过，无跳过\n2. 覆盖率 >= 85%（通过 pnpm coverage 验证）\n3. 使用 TypeScript 编写，有完整类型注解\n4. 测试描述清晰（describe/it 嵌套合理）\n5. 每个测试独立，不依赖其他测试的副作用\n6. 没有硬编码的私钥或助记词"
    },
    {
      "name": "C-02-coursemanager-tests",
      "coding_prompt": "在 packages/contracts/test/CourseManager.test.ts 中为 CourseManager.sol 编写完整单元测试。\n\n合约位置: packages/contracts/contracts/CourseManager.sol\n测试框架: Hardhat + ethers.js + Chai\n\n必须覆盖以下用例:\n1. createCourse: price=0 时 revert InvalidPrice\n2. createCourse: 成功创建，courseCount 递增，emit CourseCreated 事件\n3. createCourse: getAuthorCourses 返回正确数组\n4. updateCourse: 非作者调用 revert NotCourseAuthor\n5. updateCourse: 作者成功更新，emit CourseUpdated\n6. deactivateCourse: 非作者调用 revert，作者成功下架\n7. purchaseCourse: 买家 MT 余额不足（未 approve）时 revert\n8. purchaseCourse: 成功购买，MT 从买家转移到作者，emit CoursePurchased\n9. purchaseCourse: 重复购买同一课程 revert AlreadyPurchased\n10. purchaseCourse: 已下架课程 revert CourseNotActive\n11. purchaseCourse: 重入攻击场景（构造恶意合约）被 ReentrancyGuard 阻止\n12. checkPurchase / getUserPurchases / getAuthorCourses 查询结果正确\n\n运行命令: cd packages/contracts && pnpm test",
      "acceptance_criteria": "1. 所有 12 个测试场景均有对应测试并通过\n2. 覆盖率 >= 85%，purchaseCourse 函数 100% 行覆盖\n3. 重入攻击测试必须包含真实的 ReentrancyAttacker 合约模拟\n4. 使用 loadFixture 避免重复部署（性能优化）\n5. 事件验证使用 emit().withArgs() 精确匹配参数\n6. 所有 revert 验证使用 revertedWithCustomError() 而非字符串匹配"
    },
    {
      "name": "B-01-auth-api",
      "coding_prompt": "实现 apps/api/src/routes/auth.ts 中签名登录 API，取消所有 TODO 注释，接入真实的 Prisma + Supabase 操作。\n\n技术栈:\n- Hono v4（路由框架）\n- Prisma Client（ORM，通过 packages/prisma/src/client.ts 初始化）\n- viem verifyMessage（签名验证，已在 apps/api/src/services/signature.ts 实现）\n- hono/jwt（JWT 签发）\n\n需要实现:\n1. GET /auth/nonce: 生成 UUID nonce，写入 Supabase auth_nonces 表（expiresAt = now + 5分钟），返回 { nonce, message }\n2. POST /auth/verify: \n   a. 从 auth_nonces 表查询 nonce，验证未过期且 usedAt 为 null\n   b. 调用 verifySignature(address, nonce, signature) 验证签名\n   c. 在事务中: UPSERT users 表 + 更新 nonce.usedAt = now\n   d. 签发 JWT（payload: sub=address, exp=24h, jti=uuid）\n   e. 通过 Set-Cookie 返回 HttpOnly JWT\n3. POST /auth/logout: 清除 Cookie\n\n错误处理:\n- nonce 不存在/已过期: 400 { error: 'Invalid or expired nonce' }\n- 签名验证失败: 401 { error: 'Invalid signature' }\n- 所有错误响应格式统一: { error: string }\n\nPrisma client 初始化方式参考 packages/prisma/src/client.ts，DATABASE_URL 从 c.env.DATABASE_URL 获取。",
      "acceptance_criteria": "1. GET /auth/nonce 返回有效的 UUID nonce 和签名消息\n2. auth_nonces 表写入成功，expiresAt 为当前时间 +5 分钟\n3. POST /auth/verify 签名验证使用 viem verifyMessage，不使用 ethers.js\n4. nonce 验证后立即标记 usedAt，防止重放攻击\n5. users 表 UPSERT 正确（首次登录创建，再次登录不报错）\n6. JWT Cookie 设置 HttpOnly + Secure + SameSite=Strict + Max-Age=86400\n7. 重放 nonce（usedAt 已设置）时返回 400\n8. 所有数据库操作有 try/catch，错误有日志输出\n9. 无硬编码的 JWT 密钥，从 c.env.JWT_SECRET 读取"
    },
    {
      "name": "B-02-courses-api",
      "coding_prompt": "实现 apps/api/src/routes/courses.ts 中课程管理 API，接入 Prisma + Supabase。\n\n需要实现的接口:\n1. GET /courses: 分页查询 courses 表（isActive=true），支持 query params: page, pageSize, search（标题模糊匹配）, sort（createdAt_desc/price_asc/price_desc），返回 { total, page, pageSize, items: CourseListItem[] }\n2. GET /courses/:id: 查询单条课程，join 作者 nickname，返回 CourseDetail\n3. POST /courses（需 JWT）: 创建课程元数据，写入 Supabase courses 表，body 包含 { title, description, price（字符串 bigint）, metaHash, coverImageKey, contentKey }\n4. PUT /courses/:id（需 JWT）: 验证 authorAddress === JWT.sub，更新课程，支持修改 title/description/price/metaHash\n5. DELETE /courses/:id（需 JWT）: 验证作者身份，软删除（isActive=false）\n\n注意:\n- price 字段存储为 BigInt，API 返回时序列化为字符串\n- 所有 JWT 鉴权接口从 Cookie ml_session 读取 token\n- 使用 Zod 做请求体验证\n- 分页默认 page=1, pageSize=20，最大 pageSize=100\n\n类型参考 packages/types/src/course.ts",
      "acceptance_criteria": "1. GET /courses 分页逻辑正确（skip/take），total 返回总数\n2. GET /courses 搜索功能使用 Prisma contains 而非 SQL 注入风险写法\n3. POST /courses price 字段: 接受字符串，存储为 BigInt，返回为字符串\n4. PUT /courses/:id 非作者操作返回 403\n5. DELETE /courses/:id 软删除，记录未物理删除\n6. 所有接口 Zod 验证失败返回 400 和具体字段错误信息\n7. BigInt 序列化时不报 JSON.stringify 错误（需自定义 replacer 或 toString）\n8. 查询使用索引字段（authorAddress, isActive, createdAt）"
    },
    {
      "name": "B-03-r2-presigned-url",
      "coding_prompt": "实现 apps/api/src/services/r2.ts 中真实的 R2 预签名 URL 生成，替换占位实现。\n\n使用 @aws-sdk/client-s3 和 @aws-sdk/s3-request-presigner 接入 Cloudflare R2（兼容 S3 API）。\n\n需要实现:\n1. createR2Client(env: Env): S3Client — 初始化 R2 S3 兼容客户端，endpoint 从 env 读取\n2. generateDownloadUrl(client, bucket, key, expiresIn=900): Promise<string> — 生成下载预签名 URL\n3. generateUploadUrl(client, bucket, key, contentType, expiresIn=3600): Promise<string> — 生成上传预签名 URL\n4. buildCourseContentKey(courseId, filename): string — 生成 R2 Object Key（格式: courses/{courseId}/{filename}）\n5. buildCourseCoverKey(courseId, filename): string — 封面图 Key（格式: covers/{courseId}/{filename}）\n\n更新 wrangler.toml，添加 R2_ACCOUNT_ID、R2_ACCESS_KEY_ID、R2_SECRET_ACCESS_KEY 环境变量说明（通过 wrangler secret 配置）。\n更新 apps/api/src/routes/content.ts，使用真实实现替换 TODO。",
      "acceptance_criteria": "1. S3Client 使用 Cloudflare R2 endpoint（格式: https://{accountId}.r2.cloudflarestorage.com）\n2. 下载 URL 有效期默认 900 秒（15 分钟）\n3. 上传 URL 有效期默认 3600 秒，包含 ContentType 限制\n4. Object Key 格式符合规范，不含前导斜杠\n5. 函数有 TypeScript 类型注解\n6. 客户端初始化逻辑只依赖 env 变量，无硬编码凭证\n7. 错误（如 bucket 不存在）有明确的错误信息"
    },
    {
      "name": "F-01-wallet-components",
      "coding_prompt": "实现以下前端钱包相关组件，技术栈: Next.js 14 + wagmi v2 + viem + shadcn/ui + Tailwind CSS + next-intl。\n\n1. apps/web/components/wallet/ConnectButton.tsx:\n   - 未连接: 显示「连接钱包」按钮（使用 i18n key: common.connect_wallet）\n   - 连接中: 显示 loading 状态\n   - 已连接: 显示地址缩写（前6后4字符）+ Sepolia 徽标 + 下拉菜单（个人中心/收益中心/断开连接）\n   - 未安装 MetaMask: 弹出 shadcn Dialog 引导安装\n   - 使用 wagmi: useAccount, useConnect, useDisconnect\n\n2. apps/web/components/wallet/NetworkGuard.tsx:\n   - 检测 useChainId() !== 11155111 (Sepolia)\n   - 显示橙色顶部 Banner: 「当前网络不是 Sepolia 测试网」+ 「切换网络」按钮\n   - 使用 wagmi: useSwitchChain\n   - 支持 i18n: wallet.wrong_network / wallet.switch_network\n\n3. apps/web/components/wallet/TxProgress.tsx:\n   - shadcn Sheet 底部浮层\n   - 4 种状态: pending_confirm（等待 MetaMask 确认）/ broadcasting（交易上链中）/ success / failed\n   - broadcasting 状态显示进度条 + txHash 链接（https://sepolia.etherscan.io/tx/{hash}）\n   - failed 状态显示错误原因 + 「重新发送」按钮\n   - 接收 props: status, txHash?, error?, onRetry?\n\n4. apps/web/components/layout/Navbar.tsx:\n   - Logo（MintLearn）+ 导航链接 + 语言切换 + ConnectButton\n   - 语言切换: 下拉选择 中文/English，调用 next-intl router 切换\n   - 移动端: 汉堡菜单（shadcn Sheet）\n\n所有文案从 packages/i18n/messages/zh.json 和 en.json 对应 key 读取，不允许硬编码中文或英文字符串。",
      "acceptance_criteria": "1. ConnectButton 四种状态（未连接/连接中/已连接/未安装）UI 完整\n2. 地址显示格式: 0x1234...abcd（前6后4）\n3. NetworkGuard 在 chain.id !== 11155111 时正确显示 Banner\n4. TxProgress 四种状态样式区分明确，success 为绿色，failed 为红色\n5. txHash 链接指向 Sepolia Etherscan，可点击跳转\n6. 所有文案使用 useTranslations() 读取，无硬编码字符串\n7. Navbar 移动端（< 768px）显示汉堡菜单\n8. 组件无 TypeScript 错误\n9. 导入路径使用 @/ 别名"
    },
    {
      "name": "F-02-auth-hook",
      "coding_prompt": "实现前端 SIWE 签名登录 Hook 和守卫组件。\n\n1. apps/web/hooks/useAuth.ts:\n   使用 wagmi useSignMessage + TanStack Query 实现签名登录流程:\n   - login(): async 函数，执行: GET /api/nonce → signMessageAsync → POST /api/verify\n   - logout(): 调用 POST /api/logout + wagmi disconnect\n   - isLoggedIn: boolean，通过 GET /api/users/me 探测（200 = 登录，401 = 未登录）\n   - isLoading: boolean\n   - user: { address, nickname } | null\n   \n   API 请求封装在 apps/web/lib/api.ts 中（fetch + credentials: 'include'）。\n\n2. apps/web/components/wallet/SignatureModal.tsx:\n   shadcn Dialog 组件:\n   - 标题: 「签名登录」\n   - 说明文字: 解释签名不消耗 Gas、不进行链上操作（i18n: wallet.sign_description）\n   - 「确认签名」按钮（触发 useAuth().login()）\n   - 「取消」按钮\n   - loading 状态禁用按钮\n\n3. apps/web/components/layout/AuthGuard.tsx:\n   包裹需要登录的页面:\n   - isLoggedIn = false: 显示 SignatureModal（不重定向，原地登录）\n   - isLoading: 显示 Skeleton 占位\n   - isLoggedIn = true: 渲染 children\n\n4. 更新 apps/web/lib/api.ts，封装常用请求:\n   - getNonce(address): GET /auth/nonce\n   - verifySignature(address, nonce, signature): POST /auth/verify\n   - getMe(): GET /users/me",
      "acceptance_criteria": "1. login() 完整执行 nonce → sign → verify 三步，任一步失败有 Toast 错误提示\n2. 签名消息格式与后端 buildLoginMessage() 完全一致\n3. fetch 请求携带 credentials: 'include'（Cookie 传递）\n4. isLoggedIn 状态正确：JWT Cookie 存在且 /users/me 返回 200 时为 true\n5. logout() 后 isLoggedIn 立即变为 false，页面响应式更新\n6. SignatureModal 说明文字清楚（使用 i18n）\n7. AuthGuard 未登录时原地弹出签名弹窗，不跳转\n8. useAuth 使用 useQuery 缓存用户信息，避免重复请求"
    },
    {
      "name": "F-03-course-list-detail",
      "coding_prompt": "实现课程广场页和课程详情页（只读部分）。\n\n1. apps/web/components/course/CourseCard.tsx:\n   - 封面图（Next.js Image，fallback 为占位图）\n   - 课程标题（截断超长文字）\n   - 作者昵称 + 地址缩写\n   - 价格（X MT，从字符串 bigint 转为可读格式）\n   - 购买人数\n   - 已购标识（props: isPurchased）\n   - 点击跳转 /courses/[id]\n\n2. apps/web/app/[locale]/courses/page.tsx:\n   - GET /courses 列表，使用 TanStack Query 缓存\n   - 搜索框（防抖 300ms）\n   - 排序下拉（最新/价格升序/价格降序）\n   - CourseCard 网格（桌面 3 列，平板 2 列，移动 1 列）\n   - Skeleton 加载态（6 个 CourseCard 占位）\n   - 空状态（无课程时的提示）\n   - 分页组件\n\n3. apps/web/app/[locale]/courses/[id]/page.tsx（只读部分，购买区留 placeholder）:\n   - 课程详情展示（封面大图/标题/作者/简介/价格/购买人数/上架时间）\n   - 作者信息区（头像/昵称/地址，链接到 /u/[address]）\n   - 链上存证展示（metaHash + Etherscan 链接）\n   - 右侧购买区暂时显示 '购买功能开发中' placeholder\n\n价格格式化函数: 将字符串 bigint（wei 单位）转为 '100 MT' 格式，精度保留整数（MT = bigint / 10^18）。",
      "acceptance_criteria": "1. CourseCard 在三种断点下布局正确\n2. 价格显示逻辑: '100000000000000000000' → '100 MT'\n3. 搜索防抖 300ms，避免每次输入都请求\n4. 加载态显示 Skeleton 而非空白\n5. 作者地址缩写显示（0x1234...abcd）\n6. 课程详情页 metaHash 使用 monospace 字体显示\n7. Etherscan 链接在新标签页打开（target=_blank + rel=noopener）\n8. 所有文案使用 i18n，无硬编码字符串\n9. Next.js Image 组件使用，配置域名 allow list"
    },
    {
      "name": "F-04-purchase-flow",
      "coding_prompt": "实现完整的课程购买流程组件。\n\n1. apps/web/hooks/usePurchaseCourse.ts:\n   实现 approve + purchaseCourse 原子操作:\n   - 参数: courseId(bigint), price(bigint)\n   - 使用 wagmi: useReadContract（查询 allowance）、useWriteContract、useWaitForTransactionReceipt\n   - purchase(): async → 若 allowance < price 先 approve，再调用 purchaseCourse\n   - 返回: { purchase, isPending, isApproving, isBuying, isSuccess, error }\n   - 购买成功后调用 POST /purchases 写入 Supabase\n\n2. apps/web/hooks/useMTBalance.ts:\n   - useReadContract balanceOf(address)\n   - refetchInterval: 15_000（15 秒刷新）\n   - 返回格式化后的余额字符串\n\n3. apps/web/components/course/PurchaseFlow.tsx:\n   5 种状态机:\n   - State 1 (未连接): 显示「连接钱包购买」按钮\n   - State 2 (余额不足): MT 余额显示 + 「余额不足」提示 + 「如何获取 MT」链接\n   - State 3 (未授权): 显示授权说明 + 「授权 MT 代币」按钮 + approve TxProgress\n   - State 4 (已授权未购买): 「立刻购买 X MT」按钮 + purchase TxProgress\n   - State 5 (已购买): 绿色「已购买 ✓」+ 「进入课程」按钮\n   \n   使用 useReadContract 查询 hasPurchased(userAddress, courseId)\n\n4. 更新 apps/web/app/[locale]/courses/[id]/page.tsx:\n   将 placeholder 替换为真实的 PurchaseFlow 组件。\n\n合约 ABI 和地址从 apps/web/lib/wagmi.ts 中的 CONTRACT_ADDRESSES 读取。\n注意: 所有链上金额使用 bigint，price 从字符串转为 bigint 时使用 BigInt()。",
      "acceptance_criteria": "1. allowance 不足时先触发 approve 交易，用户需在 MetaMask 中确认两次（approve + purchase）\n2. approve 成功后自动触发 purchase，无需用户再次点击\n3. 已购买状态通过 hasPurchased 链上查询确定，不依赖本地状态\n4. TxProgress 在 approve 和 purchase 阶段分别显示\n5. purchase 成功后 State 自动切换到 State 5（已购买）\n6. 金额计算全程使用 bigint，无 number 类型参与\n7. 购买成功后调用后端 API 写入 purchase_records\n8. 错误场景（余额不足/用户拒绝/网络错误）有对应 Toast 提示"
    },
    {
      "name": "F-05-profile-learning",
      "coding_prompt": "实现个人中心页和课程学习页。\n\n1. apps/web/app/[locale]/profile/page.tsx:\n   需要 AuthGuard 包裹（未登录弹出签名弹窗）\n   - 个人资料卡: 地址 + 昵称（内联编辑，PUT /users/me）+ MT 余额\n   - 昵称编辑: 点击铅笔图标 → 输入框 + 确认/取消，提交调用 PUT /users/me\n   - 已购课程列表（GET /users/me/courses），CourseCard 格式\n   - 已发布课程列表（GET /users/me/authored），含购买人数和累计收益（MT）\n\n2. apps/web/hooks/useProfile.ts:\n   - useQuery GET /users/me（staleTime: 60_000）\n   - updateNickname(nickname: string): PUT /users/me\n   - 成功后 invalidate 缓存，触发重新获取\n\n3. apps/web/app/[locale]/profile/[courseId]/page.tsx:\n   课程学习页（需 AuthGuard）:\n   - 进入时调用 POST /content/access { courseId } 获取预签名 URL\n   - 403: 重定向到课程详情页 + Toast「请先购买课程」\n   - 401: 触发重新签名登录\n   - 根据 URL 文件后缀自适应展示:\n     - .mp4: HTML5 video 标签（controls, preload=metadata）\n     - .pdf: iframe 内嵌\n     - 其他: 下载按钮\n   - 预签名 URL 在第 14 分钟（840 秒）自动静默刷新（useEffect + setTimeout）\n   - 侧边栏: 课程标题 + 作者信息 + 返回按钮\n\n4. apps/web/lib/api.ts 补充:\n   - getMyProfile(): GET /users/me\n   - getMyCourses(): GET /users/me/courses\n   - getMyAuthored(): GET /users/me/authored\n   - updateProfile(data): PUT /users/me\n   - getContentUrl(courseId): POST /content/access",
      "acceptance_criteria": "1. 个人资料昵称编辑: 提交成功后页面立即更新（乐观更新或重新 fetch）\n2. 已购课程列表分页正确，空状态有引导文字\n3. 课程学习页访问权限控制: 未购买严格 403 跳转\n4. 预签名 URL 刷新定时器在组件卸载时 clearTimeout（防内存泄漏）\n5. 视频播放不因 URL 刷新而中断（只替换 src，不重新 mount video 元素）\n6. 所有 API 请求携带 credentials: 'include'\n7. 昵称输入框有长度限制（maxLength=30）和 trim 处理\n8. TypeScript 无类型错误"
    },
    {
      "name": "F-06-earnings-aave",
      "coding_prompt": "实现收益中心页和 AAVE 质押功能。\n\n1. apps/web/hooks/useAaveStake.ts:\n   - supply(asset: `0x${string}`, amount: bigint): approve token → AAVE Pool.supply\n   - withdraw(asset: `0x${string}`, amount: bigint): AAVE Pool.withdraw\n   - 操作成功后调用 POST /aave/positions 记录\n   - 返回: { supply, withdraw, isPending, error }\n   AAVE V3 Pool 地址: 从 CONTRACT_ADDRESSES 读取（AAVE_V3_POOL）\n\n2. apps/web/hooks/useATokenBalance.ts:\n   - useReadContract: aUSDT balanceOf(userAddress)\n   - refetchInterval: 30_000（30 秒）\n   - 返回余额（bigint）和格式化字符串\n\n3. apps/web/components/defi/StakePanel.tsx:\n   - 资产选择（当前仅 USDT）\n   - 金额输入框 + 「最大」按钮（自动填入可用余额）\n   - 「一键质押」和「取回资产」两个操作区\n   - 操作进度通过 TxProgress 组件展示\n   - approve 和 supply 两步均需展示进度\n\n4. apps/web/components/defi/EarningsChart.tsx:\n   - 使用 recharts（LineChart 或 BarChart）\n   - 数据来源: GET /users/me/authored 中的每日收益数组\n   - X 轴: 日期，Y 轴: MT 收入\n   - 时间范围切换: 7 天 / 30 天 / 全部\n\n5. apps/web/app/[locale]/earnings/page.tsx:\n   - 需要 AuthGuard 包裹\n   - 顶部总览卡片（4 个指标）\n   - StakePanel + EarningsChart 并排（桌面）/ 堆叠（移动）\n   - 已发布课程收益明细表\n   - 质押记录列表（GET /aave/positions）\n\n AAVE ABI 仅需包含 supply 和 withdraw 方法，从 packages/types/src/contracts.ts 导出。",
      "acceptance_criteria": "1. supply 流程: approve USDT → AAVE Pool.supply 两步，均有 TxProgress 提示\n2. aToken 余额每 30 秒自动刷新，无需用户手动刷新\n3. 「最大」按钮正确填入可用 USDT 余额（bigint 精度）\n4. EarningsChart 时间范围切换后图表更新\n5. 质押操作成功后 POST /aave/positions 记录写入\n6. 所有金额展示保留合理精度（如 '100.5 USDT'，避免 18 位小数）\n7. AAVE 操作失败时 Toast 提示包含失败原因\n8. 页面在空数据（未发布课程/未质押）时有正确的空状态展示"
    }
  ]
}
